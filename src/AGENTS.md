1. doc/create_tables.sql is generated by a script, don't touch it.
2. Always give names to database indexes.
3. When you create a new script in resources/db/migration/ give it a number that is higher than
   the last one.
